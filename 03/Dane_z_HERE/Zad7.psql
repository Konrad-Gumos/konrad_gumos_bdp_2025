SELECT
    COUNT(DISTINCT poi.poi_id) AS liczba_sklepow_sportowych
FROM
    t2019_kar_poi_table AS poi
JOIN
    t2019_kar_land_use_a AS parks
ON
    ST_DWithin(poi.geom::geography, parks.geom::geography, 300)
WHERE
    poi.type = 'Sporting Goods Store'
AND
    parks.type = 'Park (City/County)';